name: Release

on:
  push:
    tags: [ "v*" ]

permissions:
  contents: write

jobs:
  release:
    name: Publish to Maven Central and create GitHub Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
          cache: maven
          server-id: central
          server-username: MAVEN_CENTRAL_USERNAME
          server-password: MAVEN_CENTRAL_PASSWORD
          gpg-private-key: ${{ secrets.GPG_PRIVATE_KEY }}
          gpg-passphrase: MAVEN_GPG_PASSPHRASE

      - name: Set release version from tag
        run: mvn -B -ntp versions:set -DnewVersion="${GITHUB_REF_NAME#v}" -DgenerateBackupPoms=false

      - name: Build and verify
        run: mvn -B -ntp clean verify

      - name: Publish to Maven Central
        run: mvn -B -ntp deploy -P release -Dmaven.test.skip=true
        env:
          MAVEN_CENTRAL_USERNAME: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
          MAVEN_CENTRAL_PASSWORD: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
          files: |
            outbox-core/target/outbox-core-*.jar
            !outbox-core/target/*-tests.jar
            !outbox-core/target/*-sources.jar
            !outbox-core/target/*-javadoc.jar
            outbox-persistence-jdbc/target/outbox-persistence-jdbc-*.jar
            !outbox-persistence-jdbc/target/*-sources.jar
            !outbox-persistence-jdbc/target/*-javadoc.jar
            outbox-queue-memory/target/outbox-queue-memory-*.jar
            !outbox-queue-memory/target/*-sources.jar
            !outbox-queue-memory/target/*-javadoc.jar
            outbox-queue-redis/target/outbox-queue-redis-*.jar
            !outbox-queue-redis/target/*-sources.jar
            !outbox-queue-redis/target/*-javadoc.jar
            outbox-spring-boot-starter/target/spring-boot-outbox-starter-*.jar
            !outbox-spring-boot-starter/target/*-sources.jar
            !outbox-spring-boot-starter/target/*-javadoc.jar
